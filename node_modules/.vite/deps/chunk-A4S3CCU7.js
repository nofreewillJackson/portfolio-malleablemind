// node_modules/@react95/core/esm/shared/events.mjs
var Emitter = class {
  constructor() {
    this.listeners = {};
  }
  on(eventName, callback) {
    if (!this.listeners[eventName]) {
      this.listeners[eventName] = [];
    }
    this.listeners[eventName].push(callback);
  }
  off(eventName, callback) {
    if (this.listeners[eventName]) {
      this.listeners[eventName] = this.listeners[eventName].filter(
        (cb) => cb !== callback
      );
    }
  }
  emit(eventName, data) {
    if (this.listeners[eventName]) {
      this.listeners[eventName].forEach((callback) => callback(data));
    }
  }
};
var ModalEvents = ((ModalEvents2) => {
  ModalEvents2["AddModal"] = "add-modal";
  ModalEvents2["RemoveModal"] = "remove-modal";
  ModalEvents2["ModalVisibilityChanged"] = "modal-visibility-changed";
  return ModalEvents2;
})(ModalEvents || {});
var modals = new Emitter();

export {
  Emitter,
  ModalEvents,
  modals
};
//# sourceMappingURL=chunk-A4S3CCU7.js.map
